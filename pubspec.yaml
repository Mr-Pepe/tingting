name: tingting
description: Automatic comparison of transcripts to originals for listening
  comprehension practice.

version: 1.0.0

homepage: https://github.com/Mr-Pepe/tingting

repository: https://github.com/Mr-Pepe/tingting

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  characters: ^1.0.0
  equatable: ^1.2.3
  file_picker: ^2.0.0
  flutter_staggered_grid_view: ^0.3.2
  flutter_tts: ^1.2.6
  just_audio: ^0.6.13
  path_provider: ^1.6.11
  provider: ^4.3.1
  shared_preferences: ^0.5.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  uses-material-design: true
